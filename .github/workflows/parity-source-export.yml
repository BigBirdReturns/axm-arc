name: Parity source export

on:
  push:
    branches:
      - agent/parity-source-export
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Package exact source
        run: |
          tar --exclude=.git -czf "$RUNNER_TEMP/axm-arc-source.tgz" .
          sha256sum "$RUNNER_TEMP/axm-arc-source.tgz" > "$RUNNER_TEMP/axm-arc-source.sha256"
      - name: Upload source
        uses: actions/upload-artifact@v4
        with:
          name: axm-arc-source
          path: |
            ${{ runner.temp }}/axm-arc-source.tgz
            ${{ runner.temp }}/axm-arc-source.sha256
          if-no-files-found: error
          retention-days: 1

  dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Package dependencies
        run: tar -czf "$RUNNER_TEMP/axm-arc-node-modules.tgz" node_modules
      - uses: actions/upload-artifact@v4
        with:
          name: axm-arc-node-modules
          path: ${{ runner.temp }}/axm-arc-node-modules.tgz
          if-no-files-found: error
          retention-days: 1
