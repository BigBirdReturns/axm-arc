# Post-v1 Arc action closure

This branch is the immutable Arc authority and creator-tool candidate coordinated with:

```text
World PR #203
World receiver 52162c757f905aae5c2383f6896de3b258e7cf8f
axm-embodied PR #15
functional custody 69b7f9a7bad5b4a94210313ca267a9b479402f09
qualified operator kit PR #205
physical acceptance issue BigBirdReturns/axm-world#204
```

The accepted action-law ancestor remains:

```text
6eef311836ee7cb3a43a94ce51f448a2699c3b04
```

Later commits on this branch add the compiler-bound offline Action Profile Forge, deterministic examples, and qualification custody. They do not replace the action simulation, trace replay, or accepted-receipt law used by World.

## Arc owns

- `axm-action-profile/1` parsing and authoring semantics;
- `axm-action-spec/1` compilation and digest identity;
- 30 Hz deterministic action simulation;
- compressed input traces and exact trace replay;
- `axm-action-receipt/1` creation and verification;
- accepted success, partial, and failure classification;
- later campaign consequence authority.

World and Unity remain presentation and provisional-execution receivers. Physical evidence cannot issue an accepted action or campaign result.

## Forge boundary

The offline Forge edits the deployed profile shape at `encounters.<challengeId>`, preserves unknown creator extensions, uses exact Arc APIs, carries compiler-proven examples, and emits deterministic static artifacts. Forge validation never substitutes for Arc compilation.

Its retained qualification artifact is:

```text
sha256:6f8a3e779d223931562e4cc3fa1225e3974291361eabff4295002dbaa2348e53
```

## Remaining acceptance

The repository and hosted authority path is complete. World issue #204 requires the actual Unity `6000.0.66f2` worktree, Windows player, Quest device session, immutable spool, exact Arc replay, accepted receipt, and completed embodied journal. The qualified offline operator kit in World PR #205 is the only execution entry point for that later work.

No action expansion, Book IV runtime, or unrelated post-v1 program belongs on this branch before that physical return verifies.