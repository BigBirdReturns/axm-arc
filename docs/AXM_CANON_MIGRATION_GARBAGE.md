# `axm-canon` migration garbage and residue ledger

This ledger names construction residue, duplicate topology, unfinished corpus work, and mechanisms that were mislabeled as ASOIAF so they do not quietly return to `axm-arc` or enter `axm-canon` under false ownership.

## Disposable lore-construction residue

The following pull requests were execution carriers, repair triggers, or superseded integration surfaces. Their product authority is zero and their files are not part of the migrated repository:

- #235, source-health repair trigger;
- #237, structured-adapter repair trigger;
- #240, private-research digest trigger;
- #241, structured-acquisition repair trigger;
- #246, superseded reconciliation integration attempt;
- #299, temporary exact-source and dependency export trigger used for the canon migration.

PR #299 is closed unmerged and its exporter refs are collapsed back to exact pre-Tier Bench source head `a424521e3a7a1c9f7c7f707298f179758daf3d27`, leaving zero workflow or trigger diff. The retained source and dependency artifacts preserve the export custody.

PR #247 remains integration lineage only. Its duplicate source is represented by the retained material heads instead of being copied as a fourth product tree. PR #245 belongs to Aperture and was never part of the canon migration.

Seventeen branch-era canon and ASOIAF workflows were construction topology. The prepared repository replaces them with three repository-owned workflows: portable canon qualification, ASOIAF qualification, and an explicit manual live canary.

## Tier Bench mechanisms that were wearing ASOIAF names

PR #249 begins a separate product line. Work orders, leases, settlements, persistent desks, workers, exchanges, supervisors, authenticated transport, certificate enrollment, credential deployment, broker services, actor runtimes, adapter process custody, Linux capability enforcement, and Windows AppContainer diagnostics are Tier Bench.

That line includes #249, #250, #251, #252, #253, #254, #255, #256, #257, #258, #260, #264, #265, #270, #273, #275, #278, #279, #280, #282, #284, #288, #294, and #298, together with their temporary carriers and diagnostics.

PR #282 is no longer publication debt. Its live ledger contains `ASOIAF_LOOPBACK_TLS_TERMINAL_RECEIPT_V2`, bound to exact head `0e90b9adf63a863f986003ec169fa81dc25514db`, permanent qualification run `31204703881`, and receipt artifact `9098010414`. It remains an orthogonal Tier Bench service donor until the renamed destination service independently qualifies.

The exact execution donor export was retained by run `31516794814`, artifact `9111377925`, ZIP digest `sha256:7ba0655985cd8d15eae45d275c23638a937eb00698b77aa4a6651e00d22ed592`. It seals terminal Linux product donor `202fd8cfb03ec038ae0da2bfedb0bc5727b12e7d`, the terminal PR #282 service donor, and diagnostic-only Windows head `2f02e948e1db34d80243831081cc50ccf4653fd4`.

The destination custody contract is staged in `BigBirdReturns/tier-bench` PR #162 at exact head `dbc0eb31620cd9ba8467b3ae827f350544a04084`. Migration qualification run `31517667757` passed on Ubuntu and Windows under Python 3.11 and 3.12, and every ordinary Tier Bench companion workflow on that head passed. The staging contract imports no source code, renames no public API, and authorizes no old-home cleanup.

PR #301 was the disposable exact-donor exporter. It is closed unmerged, and both exporter refs are collapsed to terminal product donor `202fd8cfb03ec038ae0da2bfedb0bc5727b12e7d`, leaving zero exporter or trigger diff.

The historical `asoiaf-answer-*` names are namespace pollution. The Tier Bench staging plan records eighteen intended domain-neutral names, but explicitly forbids a blind global replacement. Generic work, transport, credential, runtime, process, and capability protocols must first be separated from ASOIAF dossier, reconciliation, reviewed-answer, lore, and canon adapters.

## Evidence that is not lore

Synthetic fixtures, CI estates, workflow logs, canary responses, artifact receipts, and model-recall confidence values qualify mechanisms. They do not establish fictional facts. They remain tests or provenance unless an exact source, bounded locator, continuity, reviewer, and passing reconciliation transaction grant the record standing.

## Known unfinished corpus work

No production holder-controlled AGOT edition has been ingested. The five main novels have not been reconciled chapter by chapter. The 805 provisional candidates remain a review queue. No reader-facing, source-backed ASOIAF lore atlas has been published.

Additional orchestration does not close those corpus gaps. The next product transaction belongs in `axm-canon/asoiaf`: publish the exact prepared repository, ingest the exact AGOT edition, construct chapter and paragraph custody, reconcile the first candidate tranche, and publish a readable first lore layer.

## Cleanup admission rule

Lore branches and pull requests may be closed or deleted only after `BigBirdReturns/axm-canon` contains exact initial commit `fec0ddc9a5697d465ac46e55b7e3cb7647221952`, exact tree `7a4ecfa77dc3816a0b57328c762f632c1a42eae1`, migration tag `migration-v1`, and migration manifest SHA-256 `a3a3e62c66f14243126c9ff4ca49a9d96a13aad2b4409ebeac54d9569121a521`.

Tier Bench donor branches may be closed or collapsed only after PR #162 advances through actual generic-protocol extraction, domain-neutral renaming, independent Linux product qualification, orthogonal service qualification, clean Windows product reproduction, rollback custody, and exact source tombstones. Source custody staging alone is not destination acceptance.
