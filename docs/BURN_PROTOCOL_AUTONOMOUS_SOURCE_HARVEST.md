# Burn Protocol autonomous source harvest

## Current authority

Autonomous Burn custody is controlled by one machine-readable frontier record and one production workflow:

```text
tools/burn-protocol-source-frontier/active-frontier.json
.github/workflows/burn-protocol-e05c3-source-harvest.yml
```

The accepted product extent is through `E05-C2-P40`; the active canonical position is `E05-C3-P41`. The former E05C1 autonomous workflow, E05C1 owner/private workflow, and E05C2 workflow are retired. Git history and retained artifacts preserve their qualification and production evidence; none has a current schedule, push trigger, dispatch surface, or issue-publishing authority.

## Consolidated discovery surfaces

The active workflow owns all previously separate autonomous functions:

```text
bounded public-owner repository enumeration
explicit required repository anchors
best-effort private repositories
explicit Actions artifact IDs
explicit release-asset IDs
bounded nested ZIP inspection
exact-parent recovery
independently approved packet-set verification
SHA-256-ledgered artifact retention
issue #212 publication
```

Default required anchors are `BigBirdReturns/axm-arc` and `BigBirdReturns/axm-world`; the bounded public `BigBirdReturns` estate is unioned through `resolve_owner_scope.py`. Best-effort private custody covers `axm-chat`, `spectra-genesis`, `axm-bloodstream`, and `chatgpt-web`. Required discovery failure is fatal. Optional discovery failure is explicit and remains outside required-scope absence authority.

## Exact authority

Only the exact parent may grant parent standing:

```text
Star_Trek_Discovery_The_Burn_Protocol_Web_Series_v0.62.0.zip
641,627,846 bytes
sha256:f67dcd2c632720566e38b04c0a6b844188de24c967a77a4be31978a5ff82349a
```

An independently supplied E05C3 packet-set SHA-256 may grant transport standing under the landed packet verifier. A repository name, artifact title, URL, candidate filename, or digest learned from the same candidate cannot self-approve source.

## Result law

The active workflow preserves the existing fail-closed states:

```text
verified-frontier-evidence
verified-frontier-packet-set
source-required
source-required-packet-set
packet-set-approval-required
source-not-found
harvest-error
```

Transport refusal, incomplete required discovery, unsafe archive structure, byte mismatch, or hash contradiction cannot appear as source absence. A green `source-not-found` receipt is an audited custody result, not permission to infer Chapter 3. No workstation command is part of the operating path.
